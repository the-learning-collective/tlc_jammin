function Layers(start, end){
  this.layers = initLayers(start, end)

  function initLayers(start, end){
    var arr = []
    years = _.range(start, end, 1)
    _.each(years, function(year){
      layer = L.
        // whole bunch
      arr.push(layer)
    })
    return arr
  }
}